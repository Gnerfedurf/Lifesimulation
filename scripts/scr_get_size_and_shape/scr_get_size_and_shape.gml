// Initialize Array
var form = noone;

// Get Size
form[0] = real(string_char_at(sprite_get_name(sprite_index), 13));

// Get Size
form[1] = real(string_copy(sprite_get_name(sprite_index), 20, 2));

// Return Array
return form;